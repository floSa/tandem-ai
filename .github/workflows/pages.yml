name: Publier la page sur GitHub Pages

on:
  push:
    branches: [main]
    paths: ['site/**', 'catalog/**', 'pipeline/build_site.py']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  publier:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploiement.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      # La page se régénère depuis le catalogue : on ne publie jamais un artefact
      # qui aurait divergé de sa source.
      - name: Régénérer la page depuis le catalogue
        run: python3 pipeline/build_site.py
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deploiement
        uses: actions/deploy-pages@v4
